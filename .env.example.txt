# The Enshittification Index — Environment Configuration
# Copy this to .env and adjust values as needed

# Server port
PORT=3000

# Path to Chromium binary (for Puppeteer)
CHROMIUM_PATH=/usr/bin/chromium-browser

# Path to SQLite database file
DB_PATH=./data/enshittindex.db

# JWT secret for admin auth (change this!)
JWT_SECRET=change-me-to-a-random-string

# Google Safe Browsing API v4 key (optional - checks skipped if empty)
GOOGLE_SAFE_BROWSING_KEY=

# VirusTotal API key (optional - checks skipped if empty)
VIRUSTOTAL_KEY=
